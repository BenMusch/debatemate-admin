require "rails_helper"

feature "User can create a lesson" do
  scenario "on a date and at a school where a lesson is already scheduled" do

  end

  scenario "on a date without a lesson scheduled for that school" do

  end
end
